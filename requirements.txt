# Core
Django==5.0.7
djangorestframework==3.15.2
django-cors-headers==4.4.0
djangorestframework-simplejwt==5.3.1   # JWT authentication (remove if unused)

# Database (Supabase/PostgreSQL)
psycopg2-binary==2.9.9                 # PostgreSQL driver (binary build)
dj-database-url==2.1.0                 # Parse DATABASE_URL
python-decouple==3.8                   # Environment variable management

# Production server
gunicorn==22.0.0                        # WSGI server for deployment

# Optional utilities (install only if you use them)
requests==2.32.4                        # External API calls (e.g., Mapbox)
